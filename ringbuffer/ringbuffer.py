# Put a ring on it.
# Difficulty: Easy 1
# Category: Basics
# Topics: Basics, Data Structures, Done, Auto, Mark solved, Claim, Example 1, Input:, Output:, Accepted, Acceptance, Next up, C++ 17, INPUT:, EXPECTED:, OUTPUT:, QUESTS, Resets in 14h 15m, 30 / 240 XP · 50 / 400 coins, 0/1, 10s, 256 MB, Jan 1, 2024, Google, 3.0ms, 944 KB
# Source: https://whitebox.ac/problems/ringbuffer
# Pushed by Whitebox→GitHub Extension
capacity = cap;
        buffer.resize(capacity);
public:
    RingBuffer(int cap) {
    int count;   // current number of elements

    vector<string> buffer;
    int capacity;
    int start;   // index of oldest element
class RingBuffer {
private:
using namespace std;

#include <vector>
#include <string>
#include <iostream>